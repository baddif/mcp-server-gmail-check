[pytest]
markers =
    integration: mark tests as integration tests that require external services or credentials
    live: mark tests that contact live Gmail APIs
    slow: mark tests that are slow and should be excluded from fast CI runs
[pytest]
markers =
    integration: mark test as integration that requires external resources
    live: mark test as live (requires real Gmail credentials)
    slow: mark test as slow
